/**
 * Created by kras on 18.07.16.
 */
'use strict';

/**
 * @param {{name: String, module: String, config: {}, initMethod: String, initLevel: Number}} component
 */
function loadComponent(component, reg) {
  var constructor
}


/**
 * @param {Array} components
 * @returns {Promise}
 */
function di(components) {
  var reg = {};
  var
  for (var i = 0; i < components.length; i++) {

  }

  return new Promise(function (resolve, reject) {
  });
}

module.exports = di;
