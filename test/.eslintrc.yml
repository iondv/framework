env:
  mocha: true #  adds all of the Mocha testing global variables TODO - в папку тестов
  protractor: true # Protractor global variables TODO - в папку тестов или в каждом тесте /* eslint-env protractor, mocha */
globals:
  app: false
  chai: true
rules:
  no-console: off